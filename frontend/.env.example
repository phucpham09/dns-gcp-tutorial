# Gọi thẳng backend (CORS đã bật trên FastAPI). Để trống = dùng proxy Next.js /api/backend.
# NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000

# Đích rewrite /api/backend/* (next.config.ts).
# BACKEND_INTERNAL_URL=http://127.0.0.1:8000
